# ── Núcleo (Fase 1: bot + fuentes) ────────────────────────────────────────────
requests>=2.31.0
# El extra [job-queue] es obligatorio: sin él el bot arranca pero NO envía
# alertas automáticas (solo responde comandos).
python-telegram-bot[job-queue]>=20.7

# ── API del dashboard (Fase 2) ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0          # requerido por fastapi.testclient

# ── Predictor de riesgo (Fase 4) ──────────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0

# Nota: el mapa de incendios usa Leaflet en el frontend (ver dashboard-ui/),
# no folium — el dashboard es React + Vite, no Streamlit.
