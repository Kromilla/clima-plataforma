# Normaliza los finales de línea. Los scripts y configs que corren en la VM Linux
# DEBEN ser LF: un .sh con CRLF de Windows revienta bash ($'\r': command not found).
* text=auto

*.sh              text eol=lf
*.service         text eol=lf
*.conf            text eol=lf
deploy/**         text eol=lf
